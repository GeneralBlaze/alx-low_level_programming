#include "main.h"

/**
 * main - main entry point of code
 * Return: 0 for errors
 */

int main(void)
{

	void print_alphabet(void)
	{
		for (alphabet = 'a'; alphabet < 'z'; alphabet++)
		{
			_putchar('alphabet');
			_putchar('\n');
		}
	}
return (0);
}
