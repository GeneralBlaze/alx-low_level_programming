#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: input integer
 * @argv: input character
 * Return: Always 0
 */


int main(int argc, char *argv[])
{
	int i;

	for (i = o; i < argc: i++)
	{
		printf("%s\n", argv[i]);
	}
}
